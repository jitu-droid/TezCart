apiVersion: v1
kind: Secret
metadata:
  name: tezcart-secrets
  namespace: tezcart
type: Opaque
stringData:
  # JWT Secret for user authentication
  JWT_SECRET: "tezcart-secret-key-change-in-production"
  
  # Admin Credentials
  ADMIN_USERNAME: "admin"
  ADMIN_PASSWORD: "admin123"
